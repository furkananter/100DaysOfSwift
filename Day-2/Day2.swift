    import  Cocoa
    // hasSuffix() - son ek demek. Son ek'i .jpg ile bitiyor mu? dedik.
    
    
    let filename = "paris.jpg"
    // print(filename.hasSuffix(".jpg"))
    
    let number = 120
    // print(number.isMultiple(of: 3))
    
    
    // yukarıdaki gibi number. demek yerine 120.isMultiple(of:3) diyebiliriz.
    
    // ! 
    
    var isAuth = false
    
    isAuth = !isAuth
    // print(isAuth)
    isAuth = !isAuth
    // print(isAuth)
    
    
        // toggle()
    
    var gameOver = false
    // print(gameOver)
    gameOver.toggle()
    // print(gameOver)

// önemli ama çok
    let quote = "Then he tapped a sign saying \"Believe\" and walked away."
    //  
